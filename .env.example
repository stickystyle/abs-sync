# Audiobookshelf Sync Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# SOURCE SERVER (Triage)
# =============================================================================
# URL of the source Audiobookshelf server (no trailing slash)
SOURCE_URL=https://triage.example.com

# API key for the source server
# Get this from: Settings > Users > [Your User] > API Token
SOURCE_API_KEY=your_source_api_key_here

# =============================================================================
# DESTINATION SERVER (Main Library)
# =============================================================================
# URL of the destination Audiobookshelf server (no trailing slash)
DEST_URL=https://audiobookshelf.example.com

# API key for the destination server
DEST_API_KEY=your_dest_api_key_here

# Library ID on the destination server where books will be synced
# You can find this in the URL when viewing a library: /library/{library_id}
DEST_LIBRARY_ID=lib_your_library_id

# =============================================================================
# PATHS
# =============================================================================
# Local path where audiobooks will be downloaded
# This should be the same path that the destination server monitors
DOWNLOAD_PATH=/path/to/audiobooks

# =============================================================================
# OPTIONAL SETTINGS
# =============================================================================
# Name of the collection to sync from (default: Download)
# SOURCE_COLLECTION_NAME=Download

# Name of the collection to move synced books to (default: Synced)
# SYNCED_COLLECTION_NAME=Synced

# Path for log files (default: ./logs)
# LOG_PATH=./logs
